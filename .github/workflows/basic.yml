name: Basic Workflow (No Linter)

on:
  push:
    branches:
      - main # or your default branch
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4 

      - name: Build
        run: npm install

      - name: Deploy
        run: git config --global user.name      'pippa789'
             git config --global user.email 'allenpip341@gmail.com'
             git remote set-url origin https://x-access-token:${{secrets.GITHUB_TOKEN}}@github.com{{github.repository}}
             npm run deploy 


       
